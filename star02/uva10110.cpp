#include <bits/stdc++.h>
using namespace std;
int main() {
  long long n;
  while (cin >> n && n != 0) {
    if (sqrt(n) * sqrt(n) == n)
      cout << "yes" << endl;
    else
      cout << "no" << endl;
  }
  while (1) {
  }
}